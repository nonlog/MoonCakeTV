# ==============================================================
# MoonCakeTV Configuration (Simplified - VPS Only)
# ==============================================================

# JWT Secret (required for authentication)
# Generate with: openssl rand -hex 32
JWT_SECRET=your_random_secret_key_here

# Optional: Disable password protection (public mode)
# Set to "true" to allow anyone to access without login
# Leave unset or set to "false" to require password
# DISABLE_AUTH=true

# That's it! No database, no Redis, no Docker needed.
#
# Authentication modes:
# - DISABLE_AUTH=true: Public access (no password)
# - DISABLE_AUTH unset: Password required (set via /login page)
#
# User data (password, bookmarks, history) stored in: data/user-data.json



